fn fib(n) {
  if n <= 1 { return n; };
  return fib(n - 2) + fib(n - 1);
};

let counter = 0;
loop {
  println("fib(", counter, ") = ", fib(counter));
  let counter = counter + 1;
  if counter > 28 { break; };
}
